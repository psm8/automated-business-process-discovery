from gate.gate import Gate


class Parser:

    def parse(self, expression: str) -> Gate:
        gate = Gate(expression[0:2])

        expression = expression[3:]

        for char in model:
            if char == ",":
            elif char == "(":
            elif char == ")":
            else:
        return Gate

